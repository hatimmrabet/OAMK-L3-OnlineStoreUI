import React from "react";
import { View, Button, Text, StyleSheet, useState } from "react-native";

export default function SearchByDate() {

    return (
        <View style={styles.center}>
            <Text>Search by Date :</Text>
            
        </View>
    );
};

const styles = StyleSheet.create({
    center: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        textAlign: "center",
    },
});

